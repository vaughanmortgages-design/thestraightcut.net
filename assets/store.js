document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('[data-store-menu]');const links=document.querySelector('[data-store-links]');if(menu&&links)menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});const input=document.querySelector('[data-store-search]');if(input){const cards=[...document.querySelectorAll('[data-search-card]')];const empty=document.querySelector('[data-search-empty]');input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();let shown=0;cards.forEach(card=>{const hit=!q||card.textContent.toLowerCase().includes(q);card.classList.toggle('hidden',!hit);if(hit)shown++});if(empty)empty.hidden=shown>0})}document.querySelectorAll('[data-store-filter]').forEach(select=>select.addEventListener('change',()=>{const group=select.closest('[data-filter-group]')||document;const cards=[...group.querySelectorAll('[data-filter-card]')];const values=[...group.querySelectorAll('[data-store-filter]')].map(s=>s.value).filter(Boolean);cards.forEach(card=>card.classList.toggle('hidden',!values.every(v=>(card.dataset.filter||'').split(' ').includes(v))))}));document.querySelectorAll('[data-newsletter-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const note=form.querySelector('[data-newsletter-note]');if(note)note.textContent='Signup connection pending newsletter provider form URL.'}));document.querySelectorAll('a[href="#"]').forEach(a=>a.addEventListener('click',e=>e.preventDefault()))});